<?php
/**
 * 配置项默认值（代码级，只读）
 *
 * 这里定义的是「开箱即用的默认值」，也是配置的最终回退层。
 * 取值优先级完整链路（见 app/common/Settings.php）：
 *
 *     进程缓存 → options 表 → 环境变量(.env) → 本文件 → 调用方传入的默认值
 *
 * 为什么要单独有这么一层，而不是直接把默认值写在调用处：
 *   1. 后台可以展示「默认值 vs 当前值」，站长一眼看出哪些被改过；
 *   2. 需要「恢复默认」时，删掉数据库里那一行即可，不用去翻代码里散落的常量；
 *   3. 所有可调项集中在一处，便于对照文档。
 *
 * ═══ 三条必须遵守的约定 ═══
 *
 * 1. **只放可以公开的默认值**。任何密钥、密码都不允许出现在本文件。
 *
 * 2. **类型即契约**。写 `true` 就是布尔、写 `40` 就是整数 ——
 *    类型由这里的字面量决定（见 Settings::coerce()），
 *    它同时决定后台渲染成复选框还是输入框。写错类型会导致
 *    「存了字符串 '0' 却被当成真」这类极难排查的 bug。
 *
 * 3. **键名必须能与环境变量名互相推导**。规则是「点号转下划线并大写」：
 *    key_pool.default_rpm → KEY_POOL_DEFAULT_RPM
 *    若某天要让 .env 覆盖它，必须保证能推出这个名字。
 *
 * 分组（顶层的键）不是纯装饰：后台配置页按这个分组折叠展示，
 * 因此新增配置项时请放进合适的组，而不是都堆在顶层。
 */

declare(strict_types=1);

return [
    // ═══════════════════════════════════════════════════════
    // 站点
    // ═══════════════════════════════════════════════════════
    'site' => [
        // 站点名称，展示在后台标题与首页
        'name' => 'aqua-api-php',

        // 站点域名（**可选**，安装时可留空）。
        // 用途：展示，以及将来生成绝对地址（回调地址、邮件里的链接）时的前缀。
        // 刻意**不做 Host 头校验** —— 那样一旦填错就会把自己锁在门外，
        // 而且反代/内网访问场景下 Host 本来就与对外域名不一致
        'domain' => '',

        // 站点模式：
        //   commercial      商业站：显示定价、充值等入口
        //   public_welfare  公益站：隐藏计费入口，改为「赞助」入口，并默认收紧限流
        'mode' => 'commercial',

        // 首页的一句话介绍。留空则用代码里写死的默认文案
        'description' => '',

        // 站点公告。非空时会在首页与后台顶部显示，用于临时通知（维护、限流等）
        'announcement' => '',

        // 备案号。境内服务器必须展示，否则有被通报的风险
        'icp' => '',

        // 页脚署名。留空则显示开源项目名
        'footer' => '',

        // 首页是否公开展示「可用模型」清单。
        //   true（默认）—— 展示。访客需要知道这个站能提供什么，
        //                  这是门面页最基本的信息
        //   false        —— 不展示。若你不希望对外暴露支持哪些模型，关掉它
        //
        // 只公开模型名，不会暴露渠道名、上游地址、密钥池规模等运维信息
        'show_models' => true,

        // 首页「快速开始」示例代码里用的模型名。
        //
        // 为什么需要这一项：首页示例是按字母序取模型清单的第一个，
        // 但那多半不是你真正测通过的模型 —— 访客照抄第一段代码就报错，
        // 第一印象直接毁掉。这里填一个**你自己验证过能跑通**的模型名。
        // 留空则退回按字母序的第一个
        'demo_model' => '',
    ],

    // ═══════════════════════════════════════════════════════
    // 模型上架（拉取清单 + 测活判定）
    // ═══════════════════════════════════════════════════════
    'probe' => [
        // 只上架「确实能调用」的模型。
        //
        // 为什么需要它：NVIDIA NIM 这类免费上游，`/models` 会把账号里
        // **所有**模型都列出来，但其中相当一部分对当前账号是「没有权限」——
        // 也就是「不是免费模型」。把它们拉进清单只有坏处：
        // 用户在模型广场看到、选中、调用，然后拿到 403。
        //
        // 判定口径（很重要）：
        //   · 200            → 可用，上架
        //   · 没有权限        → **不是免费模型**，不上架、并自动下架
        //   · 超时 / 无法判定  → 仍然算可用，**保留**（免费模型只是慢，不是不能用）
        //   · 当前协议不可用  → 不上架（这个模型不认 chat/completions）
        //
        // 关掉它就退回「拉到什么就上架什么」，由站长自己挑。
        'free_only' => true,
    ],

    // ═══════════════════════════════════════════════════════
    // 网关（转发行为）
    // ═══════════════════════════════════════════════════════
    'gateway' => [
        // 与上游建立连接的超时（秒）。这个值只覆盖 TCP/TLS 握手，
        // 不覆盖「模型思考时间」—— 后者由 ttft_timeout 管
        'connect_timeout' => 8,

        // 探测类请求（测活、拉模型清单）的总超时（秒）。
        // 刻意比正式转发短：测活是给人看的，宁可快速失败
        'probe_timeout' => 20,

        // ── 流式转发的超时分层 ──
        // 分三个而不是一个，是因为它们的职责完全不同：
        //   ttft —— 首字节超时：判断上游是否「根本没响应」。
        //           这是唯一适合用来「换 Key 重试」的判据
        //   idle —— 空闲超时：两个数据块之间的最大间隔。
        //           长回答不会因为总时长而中途中止，只会因为「卡住不动」被中止
        //   total —— 总时长兜底：防止一个连接被永久占住
        //
        // 默认取 300 秒（5 分钟）。理由：免费上游（NVIDIA 免费层这类）冷启动
        // 常常要几十秒，把首字节卡在 30 秒会让一批**本来能用**的模型被判成
        // 「无响应」；而 300 秒已经足够长，再等下去对用户也没有意义了。
        // 站长可在「配置管理 → 网关」或后台的「请求超时」卡片里自行调整。
        'ttft_timeout' => 300,
        'idle_timeout' => 60,
        'total_timeout' => 300,

        // 单个请求最多重试几次（换渠道 / 换 Key 都算一次重试）。
        // 设为 0 表示不重试，失败直接返回给调用方
        'max_retries' => 2,

        // 哪些上游状态码值得**换一条渠道**重试（逗号分隔）。
        //
        // ⚠️ 这里的「重试」不是「把同一个请求再发一遍」，而是
        // **换一条渠道 / 换一把密钥重试**。这个区别很关键：
        //   · 401/403 对单个 Key 来说是「这把坏了，重发也没用」，
        //     但在多渠道路由下恰恰最该换一条 —— 换一把 Key 可能立刻就通了。
        //     同时那把失效的 Key 会被永久停用，不会反复拖后腿。
        //   · 429/5xx 属于「上游现在忙/抖了」，换一条渠道同样有意义。
        // 所以默认值把这些都包含进来。若站长希望某些错误直接透传给用户
        // （比如想让调用方看到上游原话），把它们从这里去掉即可。
        'retry_status' => '401,403,429,500,502,503,504',

        // SSE 心跳间隔（秒）。经过 Nginx 等反代时，
        // 长时间没有数据流动的连接会被中间设备掐断 —— 必须靠心跳保活，
        // 而不是调大超时（详见项目文档里「长连接 5 分钟掉线」那一条）
        'heartbeat_interval' => 15,

        // ── 坏模型自动摘除（运行期健康度）──
        //
        // 为什么需要：生产实测过这样一组数据 ——
        //   · deepseek-ai/deepseek-v4.1-flash   20 次调用 **全部**失败，平均 49.9 秒
        //   · z-ai/glm-5.3                       7 次全失败，平均 61.1 秒
        //   · nvidia/nemotron-3-ultra-550b-a55b 36 次只失败 5 次，平均 8.5 秒（健康）
        // 少数几个坏模型把整站成功率压到三成，而用户每次都要陪着等几十秒
        // 才拿到一句「上游超时」—— 在用户眼里这就是「这个站大部分请求都是坏的」。
        //
        // 打开后：某模型连续失败到阈值就**暂时**不再把请求发给它，
        // 请求**立刻**拿到「该模型当前不可用」+ 现在能用的替代模型；
        // 冷却期一到自动放行一次试探，上游恢复了就自动恢复。
        'model_health_enabled' => true,

        // 连续失败几次算「这个模型坏了」。
        // ⚠️ 只统计真正的**模型级**失败（连不上/超时、5xx、上游说没有这个模型）；
        // 参数错误（400）、密钥问题（401/403）、限流（429）都不算 ——
        // 把它们算进来会把健康模型误摘掉，那比不摘更糟
        'model_fail_threshold' => 3,

        // 判定不可用后的冷却秒数，到期自动放行试探
        'model_cooldown_seconds' => 600,

        // ── 请求体字段适配 ──
        //
        // 有些客户端会带自己的私有字段（实测遇到 `dsh_plugin_packages`），
        // 上游不认识就直接整条请求 400 —— 用户看到的是「上游返回 HTTP 400」，
        // 完全不知道是自己客户端多带了一个参数。我们的转发本来只是搬字节，
        // 但「让各家客户端都能用」正是网关该做的事。
        //
        // 打开：只把 OpenAI 标准字段（+ 渠道「附加请求体参数」里声明的）发给上游，
        //       其余字段剥掉并记一条日志（便于发现需要保留的字段）
        'strip_unknown_params' => true,

        // 额外保留的请求字段名（逗号分隔）。
        // 某些上游支持非标准字段（例如各家自己的 thinking / reasoning 参数）时填这里
        'keep_body_params' => '',
    ],

    // ═══════════════════════════════════════════════════════
    // 密钥池（多 Key 轮换与健康治理）
    // ═══════════════════════════════════════════════════════
    'key_pool' => [
        // 单把 Key 的默认每分钟请求上限。
        // 渠道的 rpm_limit 与单把 Key 的 rpm_limit 都为 0 时才用它。
        // NIM 免费层官方口径约 40 RPM/模型
        'default_rpm' => 40,

        // 连续失败多少次后自动停用该 Key。
        // 注意只统计**瞬时失败**（429/5xx/网络抖动）；
        // 认定凭据失效（401/403）时是立即停用，不看这个阈值
        'fail_threshold' => 5,

        // 是否允许「达到失败阈值后自动停用」。关掉后只计数不停用，
        // 适合排查阶段（避免误停导致可用 Key 越用越少）
        'auto_disable' => true,

        // 自动停用后的冷却秒数：到期自动恢复启用，再给一次机会。
        // 设为 0 表示不自动恢复（需人工启用）
        'cooldown_seconds' => 600,
    ],

    // ═══════════════════════════════════════════════════════
    // 注册（下游用户的准入控制）
    // ═══════════════════════════════════════════════════════
    'register' => [
        // 是否开放注册。关掉后注册页会显示「暂未开放」，只能由站长手工开号
        'open' => false,

        // 是否需要邮箱验证。开启前请先在下面把邮件服务配好 ——
        // 否则用户注册完收不到邮件，会卡在「邮箱尚未验证」进不来
        'need_verify' => true,

        // 邀请码。非空时注册必须填对才能通过。
        // 公益站/内测阶段常用它控制入口
        'invite_code' => '',

        // 注册前是否验证邮箱「真实存在」。
        //
        // 为什么值得做：邮件服务商会统计无效地址率与退信率，
        // 指标一高整条发信通道会被降级甚至封禁 —— 那时连真实用户
        // 都收不到验证邮件，恢复还要申诉养信誉。
        // 所以宁可少发，也不要往可疑地址发。
        //
        // 验证分四级：语法 → 一次性邮箱 → 域名 MX → SMTP 收件人探测。
        // 只有**确凿判定「不存在」**才拒绝；灰名单、限流、对方因我们
        // IP 信誉而拒绝等「判不了」的情况一律放行（详见 EmailVerifier 的说明）。
        'verify_email' => true,

        // 是否做最后那一步「SMTP 收件人探测」（连到对方收信服务器直接问）。
        // 这是唯一能确认单个地址存在的手段，但需要本机 25 端口能出站。
        // 若服务器封了 25 端口（云厂商常见），关掉它仍能靠前三级挡掉大部分无效地址
        'verify_smtp' => true,

        // 是否拒绝临时 / 一次性邮箱。
        // 这类地址「收得到信但用户拿不到」，会拉高投诉率，通常不值得放进来
        'block_disposable' => true,

        // 追加的一次性邮箱域名黑名单（逗号或空格分隔）。
        // 内置了一份常见的，这里放站长自己遇到的 —— 这类域名每天都在新增，
        // 靠内置列表永远追不完
        'extra_blocked_domains' => '',

        // 注册赠送余额。0 表示不赠送。
        // 注意这是**真金白银**（用户可用来消耗你的上游额度），
        // 开放注册时建议配合「令牌默认额度」一起收紧
        'gift_balance' => 0.0,

        // 新用户注册时自动创建的令牌额度上限。0 表示不限（谨慎使用）
        'default_token_quota' => 0.0,
    ],

    // ═══════════════════════════════════════════════════════
    // 邮件（SMTP）
    // ═══════════════════════════════════════════════════════
    'mail' => [
        // 是否启用发信。关闭时注册/找回密码流程仍会走完，
        // 只是不发邮件（并把失败原因记进日志）
        'enabled' => false,

        // SMTP 服务器，例如 smtp.qq.com / smtp.163.com / smtp.exmail.qq.com
        'host' => '',

        // 端口：SSL 通常 465，STARTTLS 通常 587
        'port' => 465,

        // 加密方式：ssl（连接即加密）| tls（STARTTLS）| none（不加密，仅内网）
        'secure' => 'ssl',

        // 登录账号（通常就是发件邮箱）
        'username' => '',

        // 登录口令。⚠️ 多数邮箱要填的是**授权码**而不是登录密码。
        // 此项加密存储（见 Settings::SECRET_KEYS），后台只显示「已配置」
        'password' => '',

        // 发件人地址。必须与 SMTP 账号匹配，否则会被判为伪造发件人
        'from' => '',

        // 发件人显示名。留空则用站点名称
        'from_name' => '',
    ],

    // ═══════════════════════════════════════════════════════
    // 支付（易支付 V1 / V2）
    // ═══════════════════════════════════════════════════════
    'payment' => [
        // 是否启用在线充值
        'enabled' => false,

        // 协议模式：
        //   epay_v1  页面跳转型（/submit.php），兼容性最好
        //   epay_v2  API 型（/mapi.php），返回 JSON 里的支付链接
        // 两者在「签名是否保留空值」上约定不同，已按各自约定内置
        'gateway' => 'epay_v1',

        // 支付网关地址，例如 https://pay.example.com（不要带路径）
        'api_url' => '',

        // 商户 PID
        'pid' => '',

        // 商户密钥。⚠️ 加密存储，后台只显示「已配置」
        'key' => '',

        // 接口路径。留空用协议默认（/submit.php 或 /mapi.php）；
        // 各家实现路径不一，对不上时在这里覆盖
        'submit_path' => '',

        // 允许的支付方式（逗号分隔）：alipay / wxpay / qqpay / bank
        'pay_types' => 'alipay,wxpay',

        // 单笔最低充值金额
        'min_amount' => 1.0,

        // 到账倍率：充值 1 元 = 多少余额。做活动时调大即可
        'credit_rate' => 1.0,
    ],

    // ═══════════════════════════════════════════════════════
    // 计费（上游成本与下游售价）
    // ═══════════════════════════════════════════════════════
    'billing' => [
        // 货币单位，仅用于界面展示。本项目**不做汇率换算** ——
        // 换算需要实时汇率，而汇率是外部依赖，会让「账面数字」
        // 随第三方接口波动。要换币种请自己按统一口径填写价格。
        'currency' => 'CNY',

        // 默认倍率：当某个模型只填了上游成本、没填下游售价时，
        // 下游售价 = 上游成本 × 这个倍率。
        // 注意必须是小数写法（1.0 而不是 1），否则会被当成整数截断
        'default_multiplier' => 1.0,

        // token 估算的校准系数。仅在上游不返回 usage、且渠道开了估算时生效。
        // 觉得估算偏低就调大（如 1.2），偏高就调小
        'estimate_ratio' => 1.0,

        // 没有在「模型定价」里配置价格的模型，是否允许免费放行。
        //   true（默认）—— 放行，成本记 0。适合前期：先把服务跑起来，
        //                  定价慢慢补
        //   false        —— 拒绝。适合商业站：绝不允许有算不出钱的流量
        'unpriced_is_free' => true,

        // 用户余额为 0 时是否拒绝调用。
        //   true（默认）—— 拒绝。商业站必须如此，否则等于免费送额度
        //   false        —— 放行。适合公益站，或「只做限流不做计费」的场景
        'require_balance' => true,

        // 用量日志保留天数，超期由维护脚本清理。
        // 日志表是唯一会无限增长的表，不设上限迟早拖慢查询与备份
        'log_retention_days' => 90,
    ],

    // ═══════════════════════════════════════════════════════
    // 安全
    // ═══════════════════════════════════════════════════════
    'security' => [
        // ── 令牌「随时复制」──
        //
        // 开着：新建令牌时额外存一份**可逆**副本（AES-256-GCM，用 APP_KEY），
        //       用户在控制台上可以随时复制自己的令牌。
        // 关掉：只存 SHA-256 哈希，令牌除了创建那一刻再也看不到
        //       （原来的口径：库被拖走也无法直接拿去调用）。
        //
        // ⚠️ 代价必须说清楚：开启后，**数据库 + APP_KEY 同时泄露**，
        // 拿到的令牌就是可直接使用的凭证。关掉则没有这个风险。
        // 关掉之后，已保存的副本不会自动消失，可在后台「清空已保存的令牌副本」清一次。
        //
        // 默认开：站长反馈「掉了令牌就得新建一把、再去各处改配置」太折腾，
        // 而绝大多数站点里用户自己的令牌本来就在他手里，回显并不额外泄露什么。
        'token_reveal' => true,

        // 后台登录连续失败多少次后锁定。
        // 后台只有一个入口且不需要用户名，没有锁定等于把门敞开
        'login_max_attempts' => 5,

        // 锁定时长（分钟）
        'login_lock_minutes' => 15,

        // ── 下游用户的登录失败锁定（与后台分开配置）──
        //
        // 为什么不和上面两条共用一个值：后台只有一个账号，可以配得很严；
        // 而用户是**一个群体**，阈值太严会把「单纯记错密码的正常用户」
        // 大面积挡在门外，而他们只会看到「尝试次数过多」这句莫名其妙的话。
        // 所以用户侧默认更宽松（10 次而不是 5 次）。
        'user_login_max_attempts' => 10,
        'user_login_lock_minutes' => 15,

        // 后台登录态的最长有效时长（分钟）。超过后强制重新登录。
        //
        // 为什么不直接改会话本身的 lifetime：
        //   那是「会话数据在服务器上存活多久」，属于存储层参数，
        //   在 config/session.php 里、进程启动时读一次；
        //   而这里是「管理员多久必须重新输一次密码」，属于安全策略，
        //   由中间件在每个请求上判定 —— 因此后台改完立刻生效。
        //   两者混为一谈会导致「后台改了没反应」的困惑。
        'admin_session_minutes' => 120,
    ],

    // ═══════════════════════════════════════════════════════
    // 用户（编号维护）
    // ═══════════════════════════════════════════════════════
    'users' => [
        // 是否自动补齐用户编号（把空位填掉，编号始终是连续的 1..N）。
        //
        // 背景：用户注销后编号会留下空位（例如 1、3、5），
        // 站长与用户看到「怎么没有 2 号」容易以为系统丢了数据。
        // 开启后由后台维护进程定期检查并补齐。
        //
        // ⚠️ 代价：补齐会把编号重排（5 号可能变成 3 号），
        // 所有已登录用户需要重新登录一次（这是安全要求，见 User::idEpoch 的说明）。
        // 因此只在**确实存在空位**时才动手，平时不会打扰任何人。
        'auto_compact_ids' => true,

        // 每隔多少天检查一次（检查到没有空位就什么都不做）
        'compact_interval_days' => 3,

        // ── 以下两项由系统自动维护，不用手改 ──
        // 编号代际：每次重排 +1，用来让重排前建立的会话失效
        'id_epoch' => 0,
        // 上次重排时间（Unix 时间戳）
        'last_compacted_at' => 0,
    ],

    // ═══════════════════════════════════════════════════════
    // 会话
    // ═══════════════════════════════════════════════════════
    // 键名刻意定为 session.secure 而不是 security.session_secure ——
    // 因为环境变量的映射规则是「点号转下划线并大写」，
    // 只有 session.secure 才能正确映射到 .env 里的 SESSION_SECURE
    // （config/session.php 读的正是这个变量名）。
    // 配置键与环境变量名保持一致，可以避免「改了 .env 却读不到」这类困惑。
    'session' => [
        // 会话 Cookie 是否只允许 HTTPS 传输。
        // 生产必须为 true；本地用 http://127.0.0.1 开发时需在 .env 里设为 false，
        // 否则浏览器不会保存 Cookie、登录态无法保持。
        'secure' => true,
    ],
];
