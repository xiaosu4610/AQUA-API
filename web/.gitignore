# 依赖目录：体积大且可由 package-lock.json 完整还原，不入库
node_modules/

# 构建产物：不入库
#
# 原因：
#   1) 产物文件名带内容哈希，每次构建都会产生一批新文件，入库会制造大量无意义差异；
#   2) 产物可由源码确定性重建，入库属于冗余；
#   3) 长期积累会让仓库历史迅速膨胀。
#
# 但 Go 侧用 go:embed 嵌入 dist，若该目录完全不存在会导致编译失败，
# 因此保留一个 PLACEHOLDER.txt 占位（见该文件说明）。
dist/*
!dist/PLACEHOLDER.txt
